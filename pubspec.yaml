name: cmd_final
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  firebase_core: ^4.9.0
  cloud_firestore: ^6.4.1

  # --- Navigation ---
  go_router: ^14.8.1        # ← ДОБАВЛЕНО ТОБОЙ

  # --- Networking (Chopper + HTTP) ---
  chopper: ^8.0.0
  http: ^1.2.0

  # --- Local Database (Drift) ---
  drift: ^2.22.1
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.4
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # --- Code Generation ---
  build_runner: ^2.4.9
  drift_dev: ^2.22.1
  chopper_generator: ^8.0.0

flutter:
  uses-material-design: true